<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="MLBInfo.Views.PlayerPage">
    <ContentPage.Content>
        <ListView ItemsSource="{Binding Players}" HasUnevenRows="True"
                  SelectedItem="{Binding PlayerSelected, Mode=TwoWay}">
            <ListView.Header>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="8*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <Entry Text="{Binding SearchEntry}" Placeholder="Player name" HorizontalOptions="Start" WidthRequest="290" Grid.Column="0" Grid.Row="0"/>
                    <Label Text="Is active?" VerticalOptions="Center" Grid.Column="1" Grid.Row="0"/>
                    <Button Text="Search" Command="{Binding SearchPlayerCommand}" Grid.Column="0" Grid.Row="1"/>
                    <CheckBox IsChecked="{Binding IsActiveCheckBox}" Grid.Column="1" Grid.Row="1" HorizontalOptions="Center" VerticalOptions="Start"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding NameDisplayFirstLast, StringFormat='Player name: {0}'}" FontAttributes="Bold"/>
                            <Label Text="{Binding BirthCountry, StringFormat='Birth country: {0}'}"/>
                            <Label Text="{Binding TeamFull, StringFormat='Team: {0}'}"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>