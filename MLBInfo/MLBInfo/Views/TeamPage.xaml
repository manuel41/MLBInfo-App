<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:CustomizationsApp.Effects"
             mc:Ignorable="d"
             x:Class="MLBInfo.Views.TeamPage"
             Title="Teams"
             BackgroundColor="#005082">
    <ContentPage.Content>

        <ListView ItemsSource="{Binding Teams}" HasUnevenRows="True" SelectedItem="{Binding TeamSelected, Mode=TwoWay}">
            <ListView.Header>
                <Grid ColumnSpacing="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="8*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <Picker Title="Select one Seasson" TitleColor="White" TextColor="White" ItemsSource="{Binding SeassonsFromViewModelCollector}" ItemDisplayBinding="{Binding Year}" SelectedItem="{Binding SeassonSelected}"  Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"/>
                    <Label Text="{Binding WSeasson}"  TextColor="White" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"/>
                    <BoxView HorizontalOptions="FillAndExpand" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" VerticalOptions="Center" HeightRequest="1" Color="White"/>
                </Grid>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout BackgroundColor="#27496d">
                            <Label Text="{Binding NameDisplayFull, StringFormat='Team: {0}'}" FontSize="Medium" TextColor="White"/>
                            <Label Text="{Binding State, StringFormat='State: {0}'}" TextColor="White"/>
                            <Label Text="{Binding Season, StringFormat='Season: {0}'}" TextColor="White"/>
                            <Label Text="{Binding Division, StringFormat='Division: {0}'}" TextColor="White"/>
                            <Label Text="{Binding PhoneNumber, StringFormat='Number: {0}'}" TextColor="White"/>
                            <Label Text="{Binding WebsiteUrl, StringFormat='WebsiteUrl: {0}'}" TextColor="White"/>
                            <BoxView HorizontalOptions="FillAndExpand" VerticalOptions="Center" HeightRequest="1" Color="White"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </ContentPage.Content>
</ContentPage>